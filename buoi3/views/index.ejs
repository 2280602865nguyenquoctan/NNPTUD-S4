<!doctype html>
<html lang="vi">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>Products Dashboard</title>

  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    .img-thumb { width: 48px; height: 48px; object-fit: cover; border-radius: 8px; }
    .clickable-row { cursor: pointer; }
    .nowrap { white-space: nowrap; }
  </style>
</head>

<body class="bg-light">
  <div class="container py-4">
    <div class="d-flex align-items-center justify-content-between mb-3">
      <div>
        <h3 class="mb-0">Products Dashboard</h3>
        <div class="text-muted small">API proxy: /api/v1/products</div>
      </div>
      <div class="d-flex gap-2">
        <button id="btnExport" class="btn btn-outline-secondary">Export CSV</button>
        <button class="btn btn-primary" data-bs-toggle="modal" data-bs-target="#createModal">+ Tạo item</button>
      </div>
    </div>

    <div class="card shadow-sm">
      <div class="card-body">

        <div class="row g-2 align-items-end mb-3">
          <div class="col-md-5">
            <label class="form-label">Tìm theo title (onChange)</label>
            <input id="searchTitle" class="form-control" placeholder="VD: shirt, shoes..." />
          </div>

          <div class="col-md-2">
            <label class="form-label">Số dòng / trang</label>
            <select id="pageSize" class="form-select">
              <option value="5">5</option>
              <option value="10">10</option>
              <option value="20">20</option>
            </select>
          </div>

          <div class="col-md-5 d-flex gap-2 justify-content-md-end">
            <div class="btn-group">
              <button id="sortTitle" class="btn btn-outline-dark">Sort Title</button>
              <button id="sortPrice" class="btn btn-outline-dark">Sort Price</button>
            </div>
            <button id="btnReload" class="btn btn-outline-primary">Reload</button>
          </div>
        </div>

        <div class="table-responsive">
          <table class="table table-hover align-middle">
            <thead>
              <tr>
                <th class="nowrap">id</th>
                <th>title</th>
                <th class="nowrap text-end">price</th>
                <th class="nowrap">category</th>
                <th class="nowrap">images</th>
              </tr>
            </thead>
            <tbody id="tbody"></tbody>
          </table>
        </div>

        <div class="d-flex align-items-center justify-content-between">
          <div class="text-muted small" id="meta"></div>
          <div class="d-flex gap-2 align-items-center">
            <button id="prevPage" class="btn btn-outline-secondary btn-sm">Prev</button>
            <div class="small">Page <span id="pageNow">1</span></div>
            <button id="nextPage" class="btn btn-outline-secondary btn-sm">Next</button>
          </div>
        </div>

      </div>
    </div>

    <div id="toastArea" class="position-fixed bottom-0 end-0 p-3" style="z-index: 1080;"></div>
  </div>

  <!-- Detail Modal -->
  <div class="modal fade" id="detailModal" tabindex="-1">
    <div class="modal-dialog modal-lg modal-dialog-scrollable">
      <div class="modal-content">
        <div class="modal-header">
          <h5 class="modal-title">Chi tiết sản phẩm</h5>
          <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
        </div>

        <div class="modal-body">
          <div class="row g-3">
            <div class="col-md-4">
              <label class="form-label">ID</label>
              <input id="d_id" class="form-control" disabled />
            </div>
            <div class="col-md-8">
              <label class="form-label">Title</label>
              <input id="d_title" class="form-control" />
            </div>

            <div class="col-md-4">
              <label class="form-label">Price</label>
              <input id="d_price" type="number" class="form-control" />
            </div>

            <div class="col-md-4">
              <label class="form-label">Category ID</label>
              <input id="d_categoryId" type="number" class="form-control" />
            </div>

            <div class="col-md-12">
              <label class="form-label">Description</label>
              <textarea id="d_description" class="form-control" rows="3"></textarea>
            </div>

            <div class="col-md-12">
              <label class="form-label">Images (phân tách bằng dấu phẩy)</label>
              <textarea id="d_images" class="form-control" rows="2"></textarea>
            </div>
          </div>

          <div class="alert alert-warning mt-3 mb-0">
            Nếu API public chặn PUT/POST thì sẽ báo lỗi. Dashboard vẫn đúng yêu cầu.
          </div>
        </div>

        <div class="modal-footer">
          <button id="btnSaveDetail" class="btn btn-primary">Lưu (PUT)</button>
          <button class="btn btn-secondary" data-bs-dismiss="modal">Đóng</button>
        </div>
      </div>
    </div>
  </div>

  <!-- Create Modal -->
  <div class="modal fade" id="createModal" tabindex="-1">
    <div class="modal-dialog modal-lg modal-dialog-scrollable">
      <div class="modal-content">
        <div class="modal-header">
          <h5 class="modal-title">Tạo sản phẩm mới</h5>
          <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
        </div>

        <div class="modal-body">
          <div class="row g-3">
            <div class="col-md-8">
              <label class="form-label">Title</label>
              <input id="c_title" class="form-control" />
            </div>
            <div class="col-md-4">
              <label class="form-label">Price</label>
              <input id="c_price" type="number" class="form-control" />
            </div>

            <div class="col-md-4">
              <label class="form-label">Category ID</label>
              <input id="c_categoryId" type="number" class="form-control" value="1" />
            </div>

            <div class="col-md-12">
              <label class="form-label">Description</label>
              <textarea id="c_description" class="form-control" rows="3"></textarea>
            </div>

            <div class="col-md-12">
              <label class="form-label">Images (phân tách bằng dấu phẩy)</label>
              <textarea id="c_images" class="form-control" rows="2"></textarea>
            </div>
          </div>

          <div class="alert alert-warning mt-3 mb-0">
            Nếu API public chặn POST thì sẽ báo lỗi.
          </div>
        </div>

        <div class="modal-footer">
          <button id="btnCreate" class="btn btn-success">Tạo (POST)</button>
          <button class="btn btn-secondary" data-bs-dismiss="modal">Đóng</button>
        </div>
      </div>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  <script src="/public/app.js"></script>
</body>
</html>



